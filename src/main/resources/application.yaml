spring:
  datasource:
    url: jdbc:mysql://localhost:3306/url_shortener_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: Sql123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
#    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect


  security:
    oauth2:
      client:
        registration:
          github:
            client-id: Ov23liHanoFlqya00PcF
            client-secret: c97d35ad9a74bb25a6d5c87a230396e347f76ff0